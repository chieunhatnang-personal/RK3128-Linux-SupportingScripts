# RK3128 BSP Worktree

This directory is an unpacked copy of `armbian-bsp-cli-rk322x-box-current` used as a starting point for a local RK3128 BSP package.

Initial source package: `armbian-bsp-cli-rk322x-box-current` version `26.2.1`.

Layout:

- `rootfs/`: package payload that would be installed under `/`
- `DEBIAN/`: Debian control metadata and maintainer scripts
- `armbian-bsp-cli-rk322x-box-current_26.2.1_armhf.deb`: original package copy

Important: this is only extracted here. It has not been installed into the target rootfs.
